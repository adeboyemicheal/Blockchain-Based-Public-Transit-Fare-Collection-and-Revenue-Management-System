[project]
name = 'Blockchain-Based-Public-Transit-Fare-Collection-and-Revenue-Management-System'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.reduced-fare-eligibility]
path = 'contracts/reduced-fare-eligibility.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
